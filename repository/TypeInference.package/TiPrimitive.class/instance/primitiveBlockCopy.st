control primitives
primitiveBlockCopy
	"As explained in ContextPart blockCopy:, the compiler inserts 
	automatically that message in every method that contains
	a block. But that message is 'invisible' in the parse tree"
	self error: 'impossible situation'.